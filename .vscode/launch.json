{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Catalog From hello-go",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Run catalog.exe from hello-go"
    }
  ]
}
